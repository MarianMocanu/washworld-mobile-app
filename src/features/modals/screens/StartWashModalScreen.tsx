import { FC } from 'react';
import { Text, View } from 'react-native';

export const StartWashModalScreen: FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Initial start wash modal screen</Text>
    </View>
  );
};
